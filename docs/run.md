# Running QuizGen

```bash
cd quizgen
npm install
npm run dev
```

Open the exact Local URL printed by Vite.

If Vite reports that port 5173 is occupied, use the alternate port it prints, such as 5174.
